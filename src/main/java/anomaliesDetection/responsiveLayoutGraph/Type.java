package anomaliesDetection.responsiveLayoutGraph;

/**
 * A parent-child relationship exists when one web page element, the parent,is the direct container of another, the child.
 * A sibling relationship exists between two elements that share the same parent.
 */
public enum Type {
    PARENT_CHILD, SIBLING;
}
